# CrazyAppium
## 目录
util工具包  
server服务，用于启动appium服务  
base初始化driver和capbility  
page页面逻辑  
config配置文件：  
elements元素的name和定位，GetByLocator类来进行解析，直接使用这个类来获取元素的定位  
cap配置包名，activity等  
email邮件配置
CrazyPath类中定义了配置文件的路径，直接使用可以点出来需要的配置文件路径，使用ProUtil类访问配置文件